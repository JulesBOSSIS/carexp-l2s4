{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Location{% endblock %}

{% block body %}
    <main>
        <h2>Location</h2>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>DateDebutLocation</th>
                <td>{{ location.dateDebutLocation ? location.dateDebutLocation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinLocation</th>
                <td>{{ location.dateFinLocation ? location.dateFinLocation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PrixTotal</th>
                <td>{{ location.prixTotal }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_location_index') }}">Revenir à l'index de location</a>

        <a href="{{ path('app_location_edit', {'id': location.id}) }}">Modifier</a>

        {{ include('location/_delete_form.html.twig') }}
    </main>
{% endblock %}
