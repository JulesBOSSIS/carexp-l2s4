<form method="post" action="{{ path('app_voiture_location_crud_delete', {'id': voiture.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette voiture ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.id) }}">
    <button class="btn">Supprimer</button>
</form>
