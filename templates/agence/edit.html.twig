{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Modification d'une agence{% endblock %}

{% block body %}
    <main>
        <div class="container">
            <h2 class="mt-4">Modifier une agence</h2>
            <div class="row mt-4">
                <div class="col-md-6 offset-md-3">
                    <div class="card">
                        <div class="card-body">
                            {{ include('agence/_form.html.twig', {'button_label': 'Modifier'}) }}
                        </div>
                    </div>
                </div>
            </div>
            <a href="{{ path('app_agence_index') }}" class="btn btn-secondary mt-3">Revenir à la liste</a>
            {{ include('agence/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
